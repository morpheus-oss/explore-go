package solved

/*
import (
	"bufio"
	"encoding/json"
	"fmt"
	"os"
)

func main() {
	scanner := bufio.NewScanner(os.Stdin)
	myMap := make(map[string]string, 0)

	fmt.Print("Enter a name:")
	scanner.Scan()
	name := scanner.Text()

	fmt.Print("Enter an address:")
	scanner.Scan()
	address := scanner.Text()

	myMap[name] = address

	jsonData, error := json.Marshal(myMap)
	if error != nil {
		fmt.Println("Error in marshalling JSON:", error)
		return
	}

	fmt.Println(string(jsonData))
}
*/
